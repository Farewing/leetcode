/**
 * @param {number[][]} envelopes
 * @return {number}
 */
var maxEnvelopes = function(envelopes) {
  let dp = [];
  for (let i = 0; i < envelopes.length; i++) {
    dp[i] = [];
  }

  for (let i = 0; i < envelopes.length; i++) {}
};
